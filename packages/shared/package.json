{
  "name": "@exam-prep/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types, validation schemas, and constants used by backend, web, and mobile apps",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "rimraf": "^6.0.0"
  }
}
